import classes from "./Footer.module.css";

function Footer() {
  return (
    <div className={classes.position}>
      <footer className={classes.footer}>
        <p>HEllo address phone hours</p>
      </footer>
    </div>
  );
}

export default Footer;
